const formVerification = () => {
  let document
  let emailRegex = /.+@.+.\..+/;
  let digitRegex = /[0 - 9]/;
  let smallLetterRegex = /[a-z]/;
  let caps = /[A-Z]/;
  let symbolRegex = /[!@#$%^&*(),.?":{}|_<>]/;

};
